{
  "_howto": "cloudflarePagesDomain: the Cloudflare Workers domain serving site/ (built by tools/publish.js). Leave blank to fall back to the public-repo githack link. tools/campaign.js reads this on every run — no code change needed when it's updated.",
  "cloudflarePagesDomain": "cold-call-coachv3.buildscope-ai.workers.dev"
}
