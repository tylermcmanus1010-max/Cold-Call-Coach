# CHG-017 — impersonation baseline on the frozen build

## Before
security_log rows: 0
admin GET /portal/ -> 403   (403 = no access yet)

## The impersonation act
GET /admin/clients/1/open -> 302 -> /portal/
  reason prompt before the redirect:  no — it is a GET link, nothing is asked
  confirmation step:                  no

## After
admin GET /portal/ -> 200   (200 = full member portal)
admin-view banner rendered:          yes   (template _shell.html:17, g.viewing_as set in auth.py:80)
  banner text: Admin view · you are looking at Boars Head\'s portal
rows written to security_log:        0

## Write access during impersonation
POST /portal/requests while impersonating -> 302 /portal/products/MMI-D-003
  a write landed as the member: YES — MMI-Q-1001
  separate logged elevation required: no
  read-only by default:               no
  (probe rows removed)
security_log rows after the write:   0

## Against CHG-017's gate
  every impersonation session logged                       NOT MET — zero rows written
  timestamped                                              NOT MET — no row to stamp
  attributed to a named admin                              NOT MET
  reason-tagged                                            NOT MET — no reason is asked for
  read-only by default                                     NOT MET — a write succeeded
  visible in the member's security log in one page load    NOT MET — no member-facing security log surface exists
  write actions require a separate logged elevation        NOT MET

The security_log table exists in schema.sql:502 and is written by nothing.
Its only other reference is monti/purge.py:43, as an orphan-sweep target.
